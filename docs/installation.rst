.. highlight:: shell

============
Installation
============


Stable release
--------------

To install Set-Membership Filter, run this command in your terminal:

.. code-block:: console

    $ pip install sfilter

This is the preferred method to install Set-Membership Filter, as it will always install the most recent stable release.

If you don't have `pip`_ installed, this `Python installation guide`_ can guide
you through the process.

.. _pip: https://pip.pypa.io
.. _Python installation guide: http://docs.python-guide.org/en/latest/starting/installation/


From sources
------------

The sources for Set-Membership Filter can be downloaded from the `Github repo`_.

You can either clone the public repository:

.. code-block:: console

    $ git clone git://github.com/k2shah/sfilter

Or download the `tarball`_:

.. code-block:: console

    $ curl -OJL https://github.com/k2shah/sfilter/tarball/master

Once you have a copy of the source, you can install it with:

.. code-block:: console

    $ python setup.py install


.. _Github repo: https://github.com/k2shah/sfilter
.. _tarball: https://github.com/k2shah/sfilter/tarball/master
